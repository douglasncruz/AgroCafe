name: CI - Testes Automáticos AgroCafé

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: '**/package-lock.json'

    - name: Install Frontend Dependencies
      working-directory: ./agrocafe/frontend
      run: npm install

    - name: Run Frontend Tests
      working-directory: ./agrocafe/frontend
      run: npm test

    - name: Build Frontend
      working-directory: ./agrocafe/frontend
      run: npm run build

    - name: Install Backend Dependencies
      working-directory: ./agrocafe/backend
      run: npm install

    - name: Run Backend Tests
      working-directory: ./agrocafe/backend
      run: npm test

    - name: Build Backend
      working-directory: ./agrocafe/backend
      run: npm run build
